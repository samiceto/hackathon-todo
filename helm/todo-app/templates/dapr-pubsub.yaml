{{- if .Values.dapr.enabled }}
---
apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: {{ .Values.dapr.pubsub.name | default "pubsub-kafka" }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "todo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.dapr.pubsub.type | default "pubsub.kafka" }}
  version: {{ .Values.dapr.pubsub.version | default "v1" }}
  metadata:
    {{- range .Values.dapr.pubsub.metadata }}
    - name: {{ .name }}
      {{- if .value }}
      value: {{ .value | quote }}
      {{- else if .secretKeyRef }}
      secretKeyRef:
        name: {{ .secretKeyRef.name }}
        key: {{ .secretKeyRef.key }}
      {{- end }}
    {{- end }}
  # Scope to specific apps (backend and reminder-service)
  scopes:
    - backend
    - reminder-service
{{- end }}
